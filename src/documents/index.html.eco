---
layout: default
---

<% for name, enabled of @sections : %>
  <!-- <%= name.toUpperCase() %> -->
  <section class="<%= name %>" id="<%= name %>">
    <%- @partial("section/#{name}.html.eco", @) if enabled %>
  </section>
  <!-- / <%= name.toUpperCase() %> -->
<% end %>
